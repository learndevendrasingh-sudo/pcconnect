# Signaling Server Environment
PORT=3001
CORS_ORIGINS=http://localhost:3000
WEB_API_URL=http://localhost:3000
INTERNAL_API_SECRET=dev-internal-secret
REDIS_URL=redis://localhost:6379
NODE_ENV=development

# TLS/SSL — provide both to enable WSS (WebSocket Secure)
# Run `pnpm generate-certs` from the repo root to create dev certs.
# SSL_CERT_PATH=../../infrastructure/certs/dev.pem
# SSL_KEY_PATH=../../infrastructure/certs/dev-key.pem
